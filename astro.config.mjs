// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://www.cantarellalabs.com',
    // base: '/your-repo-name', // ONLY if NOT using a custom domain and repo isn't <username>.github.io
  });